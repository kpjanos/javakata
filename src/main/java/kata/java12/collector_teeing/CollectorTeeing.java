package kata.java12.collector_teeing;

import java.util.HashMap;
import java.util.List;

public class CollectorTeeing {

    /**
     * Use the Collectors.teeing() method to collect the best and worst students from a list
     * and store in a HashMap under "Best student", "Worst student" key
     *
     * @param studentList list of student
     * @return a HashMap with the Best and Worst student
     */
    public static HashMap<String, Student> getBestAndWorstStudent(List<Student> studentList) {
        return new HashMap<>();
    }
}
