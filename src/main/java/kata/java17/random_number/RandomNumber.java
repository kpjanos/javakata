package kata.java17.random_number;

import java.util.ArrayList;
import java.util.List;
import java.util.random.RandomGeneratorFactory;
import java.util.stream.Collectors;

public class RandomNumber {

    /**
     * Generate random numbers with the RandomGeneratorFactory class
     * @param algorithm name of random number generator algorithm
     * @param streamSize the number of values to generate
     * @param originNumber the least value that can be produced
     * @param boundNumber the upper bound (exclusive) for each value produced
     * @return list of random Integers
     */
    public List<Integer> getRandomIntNumbers(String algorithm, int streamSize, int originNumber, int boundNumber) {
        return new ArrayList<>();
    }
}
